{
  "name": "repo-test-sud",
  "version": "1.0.0",
  "description": "Express.js HTTP server tutorial with robustness features including error handling, graceful shutdown, input validation, and resource cleanup",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "cross-env NODE_ENV=test jest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "express": "4.18.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
