# ============================================================================
# Environment Configuration Template
# ============================================================================
# This file serves as a template for your environment configuration.
# Copy this file to .env and customize the values for your environment.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your specific values
#
# IMPORTANT: Never commit your actual .env file to version control!
# ============================================================================

# ============================================================================
# APPLICATION ENVIRONMENT
# ============================================================================
# Controls the application's operating mode and behavior
# Acceptable values: development, production, test
# - development: Enables verbose logging, self-signed certificates allowed
# - production: Enforces strict security, requires valid SSL certificates
# - test: Used for running automated tests
NODE_ENV=development

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# HTTPS_PORT: Port for the HTTPS secure server
# Default: 3443 (standard alternative HTTPS port for development)
# Production: Consider using 443 (standard HTTPS port, requires root/sudo)
HTTPS_PORT=3443

# HTTP_PORT: Port for HTTP server (used for HTTP-to-HTTPS redirects)
# Default: 3000 (common Node.js development port)
# Production: Consider using 80 (standard HTTP port, requires root/sudo)
HTTP_PORT=3000

# ============================================================================
# SSL/TLS CERTIFICATE CONFIGURATION
# ============================================================================
# Paths to SSL certificate files for HTTPS encryption
# These files are required for the HTTPS server to start
#
# For development: Generate self-signed certificates using:
#   openssl req -x509 -newkey rsa:4096 \
#     -keyout certificates/key.pem \
#     -out certificates/cert.pem \
#     -days 365 -nodes \
#     -subj "/CN=localhost"
#
# For production: Use certificates from a trusted Certificate Authority (CA)
# such as Let's Encrypt, DigiCert, or your organization's internal CA

# SSL_KEY_PATH: Path to the SSL private key file (PEM format)
# The private key must be kept secure and never shared
SSL_KEY_PATH=./certificates/key.pem

# SSL_CERT_PATH: Path to the SSL certificate file (PEM format)
# This can be a single certificate or a certificate chain
SSL_CERT_PATH=./certificates/cert.pem

# ============================================================================
# RATE LIMITING CONFIGURATION
# ============================================================================
# Protects against brute force attacks and denial-of-service (DoS) attempts
# by limiting the number of requests from a single IP address

# RATE_LIMIT_WINDOW_MS: Time window for rate limiting (in milliseconds)
# Default: 900000 (15 minutes = 15 * 60 * 1000)
# Shorter windows provide stricter limits but may affect legitimate users
RATE_LIMIT_WINDOW_MS=900000

# RATE_LIMIT_MAX: Maximum number of requests allowed per IP within the window
# Default: 100 requests per 15-minute window
# Adjust based on your application's expected traffic patterns
# Lower values = stricter security, higher values = more permissive
RATE_LIMIT_MAX=100

# ============================================================================
# CORS (Cross-Origin Resource Sharing) CONFIGURATION
# ============================================================================
# Controls which external domains can make requests to your API
# This is crucial for preventing unauthorized cross-origin access

# CORS_ALLOWED_ORIGINS: Comma-separated list of allowed origins
# Format: scheme://hostname:port (port optional for 80/443)
# Examples:
#   Single origin: http://localhost:3000
#   Multiple origins: http://localhost:3000,https://myapp.com,https://api.myapp.com
#   
# WARNING: Do NOT use '*' (wildcard) in production as it allows any origin
# For development, localhost origins are typically sufficient
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ============================================================================
# END OF CONFIGURATION
# ============================================================================
# For additional security configuration options, refer to:
# - config/security.js for Helmet.js and other security middleware settings
# - Express.js security best practices: https://expressjs.com/en/advanced/best-practice-security.html
# - OWASP Node.js Security Cheat Sheet: https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html
# ============================================================================
