# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in actual values:
#   cp .env.example .env
#
# IMPORTANT: Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Determines the runtime environment of the application.
# Options: development, production, test
# Setting to 'production' enables performance optimizations and security features.
NODE_ENV=development

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port on which the Express.js server will listen for incoming connections.
# Default: 3000
PORT=3000

# Logging verbosity level for application logs.
# Options: error, warn, info, debug
# Recommended: 'debug' for development, 'info' for production
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Database host address (user-provided environment variable)
# Example: localhost, db.example.com, 192.168.1.100
DB_Host=

# Legacy database connection string (maintained for backward compatibility)
# Example: mongodb://localhost:27017/myapp or postgresql://user:pass@host:5432/db
DB=

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# External API authentication key (user-provided secret)
# This value should be kept confidential and never committed to version control.
API_KEY=
